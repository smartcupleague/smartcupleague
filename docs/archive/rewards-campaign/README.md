# Rewards Campaign Archive

This folder preserves the removed Rewards / Freebet campaign page for historical
reference.

The campaign has ended. Do not re-enable this page without reviewing:

- frontend routing and navigation
- rewards backend grant endpoints
- `FreebetLedger` admin permissions
- production deployment and secrets

Current product behavior should keep existing freebet balances usable in match
prediction flows, but must not expose new campaign reward claims.

