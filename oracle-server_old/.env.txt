# Vara Network
VARA_WS=wss://testnet.vara.network

# Smart Contracts
ORACLE_PROGRAM_ID=0xREPLACE_AFTER_ORACLE_DEPLOY
BOLAO_PROGRAM_ID=0xREPLACE_AFTER_BOLAOCORE_DEPLOY

# Signers
# Hex seed: 0x<64 hex chars>
GATEWAY_SEED=REPLACE_WITH_GATEWAY_HEX_SEED
OPERATOR_SEED=REPLACE_WITH_OPERATOR_HEX_SEED

# Sports API (football-data.org)
SPORTS_API_KEY=
SPORTS_COMPETITION_CODE=WC              # WC = FIFA World Cup 2026
FRIENDLIES_COMPETITION_CODES=PL,CL

# Auto-feeder scheduler (ms between polls, 0 = disabled)
# Keep this 0 until contracts are deployed and match mapping is configured.
AUTO_FEED_INTERVAL_MS=0
CHALLENGE_WINDOW_MS=120000
FINALIZE_BUFFER_MS=15000

# CORS
ALLOWED_ORIGINS=https://REPLACE_WITH_FRONTEND_DOMAIN
